# Prompt Agent (Toy Project)

This project demonstrates a self-improving prompt loop.

## How it works
- `prompt.txt` is modified
- `judge.py` evaluates accuracy
- `loop.py` makes small changes and keeps improvements

## Run

```bash
python3 loop.py